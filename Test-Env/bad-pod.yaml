apiVersion: v1
kind: Pod

metadata:
  name: bad-resource-pod
  namespace: shared-apps

spec:
  containers:
    - name: nginx
      image: nginx

      resources:
        requests:
          memory: "2Gi"
          cpu: "2"

        limits:
          memory: "3Gi"
          cpu: "3"
          