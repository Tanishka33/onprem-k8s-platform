apiVersion: v1
kind: PersistentVolume

metadata:
  name: mysql-stateful-pv

spec:
  capacity:
    storage: 2Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /mnt/mysql-stateful-data